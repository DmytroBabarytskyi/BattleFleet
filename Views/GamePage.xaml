<Page
    x:Class="BattleFleet.GamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BattleFleet"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Морський бій" FontSize="24" HorizontalAlignment="Center" Margin="0,10,0,0"/>

        <!-- Top Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <Button x:Name="SaveGameButton" 
                    Content="Зберегти гру" 
                    Click="SaveGameButton_Click"
                    Margin="0,0,10,0"
                    Opacity="0.3"/>
            <Button x:Name="BackToMainButton"
                    Content="На головну"
                    Click="BackToMainButton_Click"
                    Margin="0,0,10,0"/>
            <ComboBox x:Name="ShipSelector" 
                      SelectionChanged="ShipSelector_SelectionChanged"
                      Width="150"
                      Margin="0,0,10,0">
                <ComboBoxItem Content="4-палубний" Tag="4"/>
                <ComboBoxItem Content="3-палубний" Tag="3"/>
                <ComboBoxItem Content="2-палубний" Tag="2"/>
                <ComboBoxItem Content="1-палубний" Tag="1"/>
            </ComboBox>
            <Button x:Name="SetHorizontalButton" 
                    Content="Горизонтально" 
                    Click="SetHorizontal_Click"
                    Margin="0,0,10,0"/>
            <Button x:Name="SetVerticalButton" 
                    Content="Вертикально" 
                    Click="SetVertical_Click"
                    Margin="0,0,10,0"/>
            <Button x:Name="FinishPlacementButton" 
                    Content="Завершити розстановку" 
                    Click="FinishPlacement_Click"/>
        </StackPanel>

        <!-- Main content -->
        <Grid Grid.Row="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="40">
                    <!-- Поле гравця -->
                    <StackPanel>
                        <TextBlock Text="Ваше поле" FontSize="20" HorizontalAlignment="Center"/>
                        <Grid x:Name="PlayerGrid" Width="500" Height="500">
                            <Image x:Name="PlayerOceanBackground" Stretch="UniformToFill" />
                        </Grid>
                    </StackPanel>

                    <!-- Поле комп'ютера -->
                    <StackPanel>
                        <TextBlock Text="Поле противника" FontSize="20" HorizontalAlignment="Center"/>
                        <Grid x:Name="ComputerGrid" Width="500" Height="500">
                            <Image x:Name="ComputerOceanBackground" Stretch="UniformToFill" />
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
